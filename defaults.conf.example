# defaults.conf - Local configuration for the cage environment.
# Copy to defaults.conf and customize. This file is gitignored.

# Ports to forward when no ports are given on the command line.
# Space-separated list. Leave empty for no port forwarding.
# Example: CAGE_PORTS="3000 5173"
CAGE_PORTS=""

# Git identity (used for commits inside the container).
CAGE_GIT_NAME=""
CAGE_GIT_EMAIL=""

# Network profile: none | claude | standard | full
CAGE_NETWORK="claude"

# Allowed git push remotes (space-separated URL patterns).
# Pushes to any remote not matching a pattern are blocked.
# Leave empty to allow all remotes.
# Note: only relevant for standard or full profiles — claude and none
# block git remote access at the network level.
# Example: CAGE_GIT_PUSH_REMOTES="gitlab.com/myorg/myrepo"
CAGE_GIT_PUSH_REMOTES=""

# Cached directories: workspace-relative paths stored in named Docker volumes
# instead of the bind mount. Avoids slow file watching on macOS (Colima/virtiofs).
# Each gets a named volume scoped to the project directory (by path hash).
# Example: CAGE_CACHED_DIRS=("node_modules" ".next" "dist")
CAGE_CACHED_DIRS=()

# Volume mounts (bash array of Docker -v strings).
# The workspace ($PWD:/workspace) is always mounted automatically.
CAGE_VOLUMES=(
    "$HOME/.config/tmux:/home/cage/.config/tmux:ro"
    "${USER}-cage-config:/home/cage/.claude"
    "${USER}-cage-ssh:/home/cage/.ssh"
)
