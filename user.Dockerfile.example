# Copy this file to user.Dockerfile and customize.
# These instructions are injected into the Dockerfile during build,
# after base system setup (as root, before cage user creation).
#
# Example: install helix editor and set as default
# RUN apt update && apt install -y helix
# ENV EDITOR=hx VISUAL=hx
#
# Example: install TypeScript language servers
# RUN npm install -g typescript typescript-language-server vscode-langservers-extracted
#
# Example: set tmux theme color
# ENV TMUX_THEME="#ff8c00"
